chromadb
sentence-transformers
torch           # required by sentence-transformers
numpy
pydantic
python-dotenv
fastapi
uvicorn
PyMuPDF         # PDF ingestion
tqdm            # optional but useful for indexing progress
requests        # basic HTTP for external calls if ever needed
PyYAML          # config loading if needed